import Navigation from 'components/Navigation/Navigation';
import React from 'react';

const Header = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default Header;
